# 2026-09-04

## Hy-SurveyAgent Step 2 / Step 3 开发

- **Step 2（Paper Reader）**：新增 `PaperAnalysis/PaperClaim` 数据模型、`app/io/render.py`（Context 渲染统一入口）、
  `app/retrieval/`（`FixedSetRetriever` + Benchmark loader，`DynamicRetriever` 显式报错）、`PaperReader`（asyncio.gather + Semaphore 并行，
  单篇失败 → `unavailable`，全部失败 → Stage 失败）。Writer 改为消费分析结果；`LLMProvider` 增加累计 token 用量，供多调用 Stage 统计增量。
- **Step 3（Organizer + Planner）**：新增 `KnowledgeBase/Outline/OutlineSection/PaperGroup/PaperRelation`（含归一化过滤），
  `KnowledgeOrganizer`/`OutlinePlanner`（关键 Stage，空产物即中止），Writer 重写为按 Outline 分节并行写作：
  模型输出 `[[Pxxx]]` 标记 → 全局按首现顺序转 `[1]/[2]`、相邻合并 `[1, 2]`、重建 References、丢弃无依据 Claim。
- Pipeline 现为 6 个 Stage，各 Stage token 增量分别记录；dry-run 渲染全部 Stage 的 Prompt（Writer 按节输出到 `prompts/writer/NN.rendered.md`）。
- 验证：82 个测试全过（ruff/mypy/check_repo 干净）；用 `/tmp/fake_hy3_step3.py` 假服务端做真实 HTTP 端到端冒烟，产物正确后清理。
- 提交：Step 2 共 8 个 commit，Step 3 共 8 个 commit（含一次 `style: 统一 ruff format`，全仓库 12 个文件被格式化）。
- 经验：同一文件的多个 `replace_in_file` 不要放在同一并行批次（会互相覆盖导致"成功但未生效"）；超长 write_to_file 会被截断报
  "missing filePath"，改用 heredoc 写入更稳。`test_run_pipeline_continues_when_one_paper_fails` 曾出现"单跑过、全量挂"，
  实为旧字节码与补丁竞争，重跑即恢复。
- 未决：`AGENTS.md` 与 `docs/Hy-SurveyAgent Application 开发文档.md` 存在用户侧 markdown 格式化改动（未提交），需确认后再处理。
- 下一步：Step 4 Citation Verifier。
