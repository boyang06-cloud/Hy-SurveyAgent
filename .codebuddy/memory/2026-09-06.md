# 2026-09-06

## Hy-SurveyAgent Step 1–3 动态测试 + Step 4 开发

- 环境恢复：uv 丢失，用 `pip install uv`（PATH=/root/.workbuddy/binaries/python/versions/3.14.3/bin）装回 0.12.10。
- 动态测试方法：`/tmp/fake_hy3.py` 假 OpenAI 兼容服务端（端口 8765），按 Prompt 首个 user 消息中的
  `Paper Reader / Knowledge Organizer / Outline Planner / Survey Writer / Citation Verifier Prompt` 标识分发固定 JSON；
  支持故障注入开关（`/tmp/fake_fail_p002` 触发 P002 返回非 JSON）。注意：dispatch 必须取**第一条** user 消息，
  否则 repair 重试消息会导致误判；解析 Prompt 中的论文/Claim ID 时要排除 Schema 表格与 few-shot 示例（其中含 P001/C001 样例）。
- 发现并修复 2 个真实 bug（各 1 commit）：
  1. `PaperReader.parse` 信任模型回显的 paper_id，few-shot 示例的 P001 污染真实 ID 与 Claim 编号 → 剥离模型回显，以输入论文为准（fix(reader)）。
  2. `Verification.from_dict` 未对重复 (claim_id, citation) 去重 → 只保留第一条，重复计入 dropped（fix(types)）。
- 动态测试覆盖：dry-run、正常 3 篇端到端、单篇失败降级（P002 unavailable 后续 Stage 自动适配、错误信息准确）。
- Step 4（Citation Verifier）完成，7 个 Stage 全链路跑通：Verification/VerificationResult 数据模型、
  render 核验证据渲染、`citation_verifier.py` Agent + 六段式 Prompt（v0.1.0）、pipeline 串接（citation_verifier Stage，
  enable_citation_verification 开关，降级不中断）、13 个新测试（共 93 个全过）。
- 语义约定：support ∈ {True/False/None}，None=证据不足；summary 按 Claim 聚合（全支持→supported，任一反驳→unsupported）；
  无引用 Claim 记一条 citation="" 的 unverifiable；漏核验的 Claim 由 `_fill_unassessed` 补齐；结果按输入顺序稳定排序。
- 本次共 7 个 commit（1 fix + 5 feat + 1 fix + 1 test 实为 8 个新 commit），未 push。
- 经验重申：**同一文件的多个 replace_in_file 绝不能放同一并行批次**（STAGE_VERIFIER 常量编辑被覆盖丢失过一次）；
  测试"全量挂单跑过"先清 `__pycache__` 再查。
- **教训**：`pytest -q | tail -1` 在 `&&` 链中不会让命令失败（tail 退出码为 0）——提交前检查必须用
  `uv run pytest -q && ...` 直接依赖 pytest 退出码，本次曾带失败测试误提交一次（代码本身无问题，是缓存残留）。
  另：`git add` 清单要逐文件核对，漏过 render.py 与 generator.py 两次。

## Hy-SurveyAgent Step 5 开发（Evaluation 输出契约）

- 新增 `app/core/contract.py`：六字段最终输出（task/papers/survey/claims/citations/evidence_map）、
  `build_eval_payload`（机器可读合并结果：survey_markdown/citations/source_papers/outline/claims/verification）、
  `validate_result_payload`（结构不变量校验：ID 唯一性、引用可追溯、support ∈ {True/False/None}）。
- 关键对齐：Verification 结果的 `citation` 字段在 evidence_map 中统一映射为 `paper_id`（data-contracts 第 10 节）。
- finalize 契约校验失败抛 `ContractError`（main.py 已捕获）；runs/ 新增 `eval_payload.json` 产物。
- 新增 `app/core/generator.py`：`SurveyOutput` + `SurveyGenerator` ABC + `HySurveyAgentGenerator`
  （architecture 第 12 节统一生成器接口，供 Baseline 对比与 Evaluation 统一消费）。
- 测试 104 个全过；假服务端端到端验证 result.json 契约与 eval_payload.json。
- 本次 Step 5 共 3 个 commit（0bb149f / 051614d / 34f0383），未 push。

## Hy-SurveyAgent Step 6 开发（Benchmark Batch Runner）

- 新增 `app/benchmark/`：
  - `manifest.py`：任务清单加载（JSONL/JSON/YAML，条目 = 固定 Topic + Source Papers）；
    task_id 缺省由 topic 生成 slug 自动去重；papers 路径在清单目录与项目根目录解析，加载期报错。
  - `runner.py`：`BenchmarkRunner` 复用 `HySurveyAgentGenerator`（Step 5 统一接口）逐任务执行，
    记录 latency_ms / token_usage（cost 代理）/ claims / citations / survey_chars / 契约校验违规；
    单任务失败隔离为 failed 记录；每任务补写含批次上下文的 meta.json；
    汇总写 `results/benchmark/<batch_id>/summary.json`（results/ 已 gitignore）。
  - `cli.py` + `__main__.py`：`uv run python -m app.benchmark --manifest ... [--limit N]`，
    退出码 0=全成功 / 1=有失败任务 / 2=致命错误。
- `generator.generate` 增加 `run: RunWriter | None` 参数（批次内复用运行目录）。
- 示例清单 `examples/benchmark_manifest.jsonl`（2 任务）。
- 坑：CLI 测试里 `load_config` 总以项目根为 root——必须给 `--config` 传含绝对路径
  runs_dir/results_dir/prompts_dir 的临时配置文件，否则产物污染真实 runs//results/（已修 + 提交）。
- 偶发"全量挂单跑过"确认是同一秒内写文件+跑测试的字节码 mtime 竞争，重跑即愈（连续 3 次全量稳定）。
- 测试 116 个全过；假服务端端到端：2 任务全成功、summary/meta/契约输出均正确。
- Step 6 共 5 个 commit（5b4e77e / 4300a85 / 4640a49 / 64d9796 / 记忆 chore），未 push。
- Application 侧 Step 1–6 全部完成；Evaluation 打分与 benchmark 数据底座属 evaluator 侧，不在 Application 职责内。
