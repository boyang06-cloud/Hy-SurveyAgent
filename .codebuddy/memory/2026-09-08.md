# 2026-09-08

## 真实 HY3 冒烟测试（2026-09-09 执行，记录于此）

- **真实端到端跑通**：`uv run python -m app.main --topic ... --papers examples/papers_vlm.json --limit 1`，
  7 Stage 全部成功，8 claims（6 supported / 2 unverifiable），survey 4269 字符。
- **关键修复**：`configs/config.yaml` 模型名 `hy3-default` 不存在（HTTP 400 gateway_error 400004）→
  改为 `hy3`。可用模型可用 `GET {base_url}/models` 查询（端点是 OpenAI 兼容聚合网关，含 glm/kimi/deepseek 等）。
- 真实调用性能：单 Stage 30–46 秒；1 篇论文全流程 ≈3.5 分钟，prompt ≈1.4万 / completion ≈3.3万 token。
- 质量观察：Writer grounding 约束生效（证据不足时明确写 "does not provide sufficient evidence" 而非编造）；
  但单篇论文时各 Section 内容重复度高（同句复现），多论文可缓解，后续可在 Writer Prompt/assemble 阶段加去重。
- `runs/smoke-real/` 保留了真实运行产物供检查。

## 全任务进度 Review

- 分支 feat/app 领先 origin 9 个 commit（Step 4/5/6 的全部工作），Step 1–3 已在更早会话推送；工作区干净。
- 质量门禁全绿：116 tests / ruff / ruff format / mypy（32 文件）/ check_repo（8 项）。
- 规模：app 32 个 .py 约 3800 行，tests 18 个文件，Prompts 5 个（task_analyzer.md 尚无——Task Analyzer
  阶段未实现，SurveyState.task_spec 仍是占位，属已知偏差而非缺陷）。
- 无遗留 TODO/FIXME；总 commit 49 个。
- 待办修正：原记忆中「docs/AGENTS.md 用户侧 markdown 改动未提交」已在 5d2b390 chore(docs) 解决，从 MEMORY.md 移除。
- 后续方向：evaluator 侧（打分 + benchmark/ 数据底座）、Baseline 生成器实现（SimplePromptGenerator 等）、
  TaskSpec/Task Analyzer（V1）、DynamicRetriever（V1）。
