# 2026-09-09

## Web 工作台（由 gpt6astra 编写前端）Review

新增文件（均未入库）：`app/web/`（server / service / schemas / __main__ / static{index.html,app.js,styles.css,mark.svg}）、
`app/io/workspace.py`、`tests/test_web.py`、`docs/Web 工作台.md`、`design-system/hy-surveyagent/MASTER.md`；
`pyproject.toml` 新增 fastapi / uvicorn / markdown-it-py 与 `hy-surveyagent-web` 入口，ruff exclude 加 `.agents`。

验证结果：`uv run pytest` 133 passed；ruff check/format、mypy（app/web + workspace）、check_repo 全干净；
实际启动 `python -m app.web` 冒烟：静态资源 200、/api/settings 正常、dry_run 任务跑通 7 个阶段、detail 契约正确。

架构评价：server 只做 HTTP 边界，service 复用 run_pipeline/Hy3Adapter/RunWriter，schemas 隔离 Web 契约，
workspace.py 负责受限 IO（run_path 正则 + resolve 校验、产物白名单、拒绝符号链接）。未修改任何 Agent / Prompt。

发现的问题（待用户决定是否修）：
1. `service._execute` 捕获 Exception 后不落任何错误日志，页面只显示"失败"，本地无痕迹。
2. `server.local_only` 直接赋值 `request._body`（Starlette 私有属性），脆弱。
3. 轮询 2.5s 拉 `/api/runs/{id}` 会返回全部 artifacts 全文（analyses/knowledge/draft/claims），开销偏大。
4. 前端提示 10 MB 与后端 MAX_BODY=12MB 不一致；`max_upload_bytes` 与 schemas 的 10_000_000 各自硬编码。
5. dry_run 下空的 final.md / draft.md 仍出现在"运行产物"可下载列表。
6. 证据卡片显示 paper_id，正文点击的是 `[n]`，两处标识不对照。
7. CSS 死代码：`.quiet`、`.field-label` 未使用。

## 临时文件情况

- `.agents/`（4.7 MB）：ui-ux-pro-max 的第三方设计 skill 包（brand / design / design-system / slides /
  ui-styling / banner-design / ui-ux-pro-max），仅 pyproject ruff exclude 引用，项目代码无任何依赖。
- `runs/`（已 gitignore）：`smoke-real`（9/6 旧冒烟）、`t-2026-09-09-0347d80c`、`t-2026-09-09-7146fad9`（今日 UI 调试）。
- 已清理：本次冒烟产生的 `runs/t-2026-09-09-f7956fb8`、`app/web/__pycache__/`。

## 用户决策与后续处理

用户选择：`.agents/` 保留但加入 .gitignore（供 gpt6astra 后续迭代 UI）；`runs/` 全部清空（3 个调试运行）；
review 问题 1–7 全修。

已落地的修复：
1. `service._execute` 失败时写 `web_status.json.error`（仅异常类型）+ `LOGGER.error`，不落原始信息。
2. `server.local_only` 改用公开 API `await request.body()`，前置 `content-length` 检查防超大 body。
   **重要坑**：Starlette 的 `BaseHTTPMiddleware.call_next` 内部用的是原始 request 的 `wrapped_receive`
   （读 `request._body`），传入新 `Request(scope, receive)` 无效——我曾这样改导致 POST 全部 422。
3. 新增 `GET /api/runs/{id}/status`（轻量，无 artifacts）；前端 running 时轮询它，变化时才拉完整 detail；
   抽出 `loadDetail(route, epoch, refocus)` 供 navigate 与 poll 复用。
4. 常量统一：`app/web/schemas.py` 定义 `MAX_UPLOAD_BYTES=10_000_000`、`MAX_BODY_BYTES=12_000_000`。
5. `service.detail` 过滤空产物（dry_run 的空 draft.md/final.md、空 analyses.json 不再展示）。
6. 前端加 `citationMap()` / `citationLabel()`，证据卡片与溯源面板显示正文 `[n]` 编号。
7. 删除 CSS 死代码 `.quiet`、`.field-label`。

新增测试 4 个（error 类型留痕、status 轻量一致性、空产物过滤、413 场景未单测）。
最终：pytest 136 passed、ruff、mypy、check_repo 全干净；启动冒烟确认 status 接口与空产物过滤生效，
超大 body（13 MB）被 413 拦截（服务端日志确认 413，但客户端在持续发送时会收到 RST，前端已限 10 MB 故不影响）。

## 待用户处理

工作区出现两个未跟踪新目录 `eval_dataset/`（SurveyBench + eval_data_construct.md）与
`eval_harness/eval_protocol.md`，本次未触碰，需确认是否入库。
`tests/test_pipeline.py::test_run_pipeline_continues_when_one_paper_fails` 偶发失败一次
（scripted provider 与并发读论文的顺序不确定），单独复跑通过，疑似 flaky，非本次改动引入。

## 提交拆分（用户要求）

在 feat/app 上拆为 6 个提交（均未 push）：
1. `a3e2378 build(web)`: pyproject 依赖（fastapi/uvicorn/markdown-it-py）+ `hy-surveyagent-web` 入口 + uv.lock
2. `de18c08 feat(io)`: app/io/workspace.py
3. `08334c6 feat(web)`: app/web 全部（含 static）—— 静态资源必须与 server.py 同提交，
   否则 `StaticFiles(directory=STATIC)` 因目录不存在直接抛错，整个 app 创建失败
4. `b82612c test(web)`: tests/test_web.py
5. `24e3fff docs(web)`: docs/Web 工作台.md + design-system + README + AGENTS
6. `925821a chore(repo)`: .gitignore 的 `.agents/` + pyproject ruff exclude

拆分技巧：pyproject 同文件分两块提交时，先 `cp` 备份 → `git checkout --` 还原 → 只改依赖部分 → 提交 → 再恢复完整版。
验证中间提交的方法：`git worktree add --detach /tmp/wt <commit>` + `ln -s /yb/hy3/.venv .venv`，
**注意 worktree 不会带 gitignore 的 API_key.conf，需先复制进去**，否则 benchmark CLI 测试报 ConfigError。
08334c6 补齐密钥后 116 passed，确认可独立运行。

仍未跟踪、未提交：`eval_dataset/`、`eval_harness/`（等待用户确认是否入库）、`.codebuddy/memory/`。

## 新增两个项目级 skill（eval harness / eval dataset）

依据 `eval_harness/eval_protocol.md` 与 `eval_dataset/eval_data_construct.md`，建成：

- `.codebuddy/skills/hy-surveyagent-eval-harness/`：SKILL.md + references（architecture / dimensions /
  judge-design / engineering-practices）+ scripts（init_evaluator.py 骨架生成、aggregate_scores.py
  加权聚合+Gate、check_eval_repo.py 自检）+ assets/templates（eval_config.example.yaml、
  judge_prompt_template.md、report_template.md）。
- `.codebuddy/skills/hy-surveyagent-eval-dataset/`：SKILL.md + references（source-data / dataset-schema /
  build-steps）+ scripts（init_builder.py 骨架生成、normalize_arxiv_ids.py 确定性 ID 规范化、
  validate_dataset.py 结构校验）+ assets/templates（7 个 JSON 模板 + build_config.example.yaml）。

两者边界：harness = 评分器/Judge/聚合/报告；dataset = 数据构造。共同铁律是**构造与评测分离**
（评测只读冻结 `datasets/<version>/`）。

坑与经验：
- `check_eval_repo.py` 第一版直接扫 `.md/.json/.conf` 找密钥，把 `API_key.conf` 与
  `tests/test_config.py` 的假值判成 FAIL；改为复用 app skill 的 `KEY_PATTERN + DUMMY_PATTERN`
  占位白名单、只扫 `.py/.yaml/.yml/.toml`、跳过 `API_key.conf` 本身与 `.example` 模板后干净。
- 用 `python3 .../skill-creator/scripts/quick_validate.py <skill-dir>` 校验 skill 结构（两个均 valid）。
  注意系统 python3 是 3.8，skill 脚本都带 `from __future__ import annotations` 以保证可跑。
- 已冒烟：aggregate_scores（含 `{"score": x}` 与纯数字两种输入）、normalize_arxiv_ids
  （`2308.04079v2` / `arXiv:` 前缀 / `bad-id` 全部正确处理）、validate_dataset（故意造错能报
  ERROR 并退出码 1）、两个 init 脚本在 /tmp 空仓库生成骨架且 compileall 通过；临时文件已清理。

同时把两个 skill 与两份 eval 文档写进 `AGENTS.md` §2 必读文档表，并加了"构造与评测分离"铁律说明。
